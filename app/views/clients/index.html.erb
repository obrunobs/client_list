<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client List</title>
</head>
<body>
    <h1>Client list</h1>
    <h2><% @clients = @clients.each do |c| %></h2>
    <h2><%= "Name: " + "#{c.name}" %></h2>
    <h2><%= "Age: " + "#{c.age}" %></h2>
    <h2><%= "Function: " + "#{c.function}" %></h2>
    <br>
    <% end %>
</body>
</html>